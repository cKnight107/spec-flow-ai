# 代码风格规范

## 基础格式
- 默认使用单引号。
- 语句末尾保留分号。
- 文件末尾必须有换行。
- 删除行尾多余空格和无意义空行。

## 导入与导出
- 导入顺序建议为：标准库、第三方依赖、内部包、相对路径。
- 无使用的导入必须清理。
- 一个文件应尽量只暴露一个主要职责的默认导出或具名导出集合。

## 结构风格
- 条件分支优先早返回，避免过深嵌套。
- 单个函数应保持单一职责，复杂逻辑应拆分辅助函数。
- 避免超长文件，页面、服务、类型应分层拆开。
