# AI Change Log

> 记录 AI Coding / Spec Coding 中“有效变更摘要”，不记录完整推理过程。
> 目标：可复盘、可审查、可交接。

---

## [CL-030] 修复 db-kit 实现并补齐最小测试
- 日期：2026-03-15
- 任务编号：TASK-014-DB-KIT-FIX
- Spec：
  - `docs/specs/014-db-kit/spec.md`
- 执行方式：Codex
- 执行人：AI + Human
- 状态：Done

### 1. 目标
修复 `packages/db-kit` 中阻断交付的实现问题，使包能够通过类型检查与最小单元测试，并与 `014-db-kit` 的核心规格保持一致。

### 2. 输入依据
- PRD / Spec：
  - `docs/specs/014-db-kit/spec.md`
  - `docs/specs/014-db-kit/tasks.md`
- ADR / 设计文档：
  - `docs/architecture/data-model.md`
- 额外约束：
  - `docs/rules/architecture-boundaries.md`
  - `docs/rules/testing-quality.md`

### 3. 本次改动
- 新增：
  - `packages/db-kit/src/core/*.test.ts`
  - `packages/db-kit/src/driver/health.test.ts`
  - `packages/db-kit/src/runtime/*.test.ts`
  - 本次 AI 变更记录
- 修改：
  - 修复事务实现中的类型错误与非数据库异常透传
  - 修复连接关闭后 `closed` 状态不更新的问题
  - 修复 fake client 对健康检查与关闭行为的模拟
  - 调整 README 中 telemetry 与边界说明
- 删除：
  - 无

### 4. 涉及文件
- `packages/db-kit/README.md`
- `packages/db-kit/src/core/context.test.ts`
- `packages/db-kit/src/core/errors.test.ts`
- `packages/db-kit/src/core/pagination.test.ts`
- `packages/db-kit/src/core/pagination.ts`
- `packages/db-kit/src/driver/health.test.ts`
- `packages/db-kit/src/driver/postgres.ts`
- `packages/db-kit/src/runtime/client.test.ts`
- `packages/db-kit/src/runtime/client.ts`
- `packages/db-kit/src/runtime/query.test.ts`
- `packages/db-kit/src/runtime/transaction.test.ts`
- `packages/db-kit/src/runtime/transaction.ts`
- `packages/db-kit/src/testing/fake-client.ts`
- `docs/ai-change-log/CL-030.md`

### 5. 关键决策
- `withTransaction` 仅对数据库特征明显的错误做归一化，业务异常保持原样抛出，避免吞掉调用方语义。
- savepoint 继续保留在事务接口上，但通过 Kysely 支持的原生 SQL 执行路径实现，不再依赖不存在的 API。
- fake client 的健康检查与关闭行为按运行时路径模拟，保证单测可以覆盖连接与健康检查场景。

### 6. 验证情况
- [x] typecheck
- [x] unit test
- [ ] integration test
- [x] manual check

补充说明：
- 仅验证 `@enterprise-ai-hub/db-kit` 包内能力。
- “至少一个服务接入”仍不在本次修复范围内。

### 7. 风险与未完成项
- 当前仍未完成真实服务侧接入，因此 `014` 规格中的集成验收项仍待后续落地。
- savepoint 仅做了实现修复，本次未引入真实数据库集成测试验证 PostgreSQL 行为。

### 8. 人工介入记录
- 是否人工修改：否
- 人工确认点：
  - 后续是否需要在某个 `services/*` 中正式接入 `db-kit`
- 需要复审点：
  - 是否需要补充 NestJS provider / helper 以贴近规格推荐路线

### 9. 结果摘要
`packages/db-kit` 已修复类型错误、生命周期缺陷与测试缺口，现可通过包级类型检查和最小单元测试，但服务侧接入验收仍待后续完成。
