# AI Change Log

## [CL-007] 初始化 model-gateway 依赖基线
- 日期：2026-03-13
- 任务编号：TASK-007
- Spec：`docs/specs/001-model-gateway/spec.md`
- 执行方式：Codex
- 执行人：AI + Human
- 状态：Done

### 1. 目标
为 `services/model-gateway` 建立首批服务级依赖声明和 TypeScript 编译基线。

### 2. 输入依据
- PRD / Spec：`docs/specs/001-model-gateway/spec.md`
- ADR / 设计文档：`docs/architecture/tech-stack.md`
- 额外约束：`services/model-gateway/AGENTS.override.md`

### 3. 本次改动
- 新增 `services/model-gateway/package.json`
- 新增 `services/model-gateway/tsconfig.json`
- 新增 `services/model-gateway/src/index.ts`
- 新增 `pnpm-lock.yaml`
- 更新 `.gitignore`

### 4. 涉及文件
- `.gitignore`
- `pnpm-lock.yaml`
- `services/model-gateway/package.json`
- `services/model-gateway/tsconfig.json`
- `services/model-gateway/src/index.ts`
- `docs/ai-change-log/CL-007.md`

### 5. 关键决策
- 先只引入 NestJS 服务骨架所需的最小依赖，避免在未明确实现范围前过量安装。
- 将依赖收敛在 `services/model-gateway`，不把服务私有依赖提升到仓库根目录。
- 增加最小 `src/index.ts`，确保 TypeScript 配置具备可验证输入。

### 6. 验证情况
- [ ] lint
- [x] typecheck
- [ ] unit test
- [ ] integration test
- [x] manual check

补充说明：
- 已执行 `pnpm install` 并生成 `pnpm-lock.yaml`。
- 已执行 `pnpm --filter @enterprise-ai-hub/model-gateway-service typecheck`。
- 安装过程中 `@nestjs/core`、`esbuild` 的 build scripts 被 pnpm 默认拦截，后续如需放行可执行 `pnpm approve-builds`。

### 7. 风险与未完成项
- 后续若引入 Nest 装饰器控制器和配置模块，可能还需补充 `@nestjs/config` 等依赖。
- 当前仅完成服务级依赖基线，尚未落业务源码与测试依赖。

### 8. 人工介入记录
- 是否人工修改：否
- 人工确认点：确认首批依赖是否以 `model-gateway` 为起点继续推进。
- 需要复审点：NestJS 版本与后续服务模板是否统一。

### 9. 结果摘要
`services/model-gateway` 已具备最小依赖声明、锁文件和通过验证的 TypeScript 包结构，可进入后续骨架开发阶段。
