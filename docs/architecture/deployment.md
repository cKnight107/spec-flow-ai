# 部署设计

## 环境划分
- `local`：单机开发与联调
- `dev`：功能验证与集成测试
- `staging`：预发布验证
- `prod`：生产环境

## 部署单元
- `apps/*` 以前端应用或边缘服务形式部署
- `services/*` 以独立容器或 K8s Deployment 部署
- `integrations/*` 作为适配器包或独立 sidecar / worker 部署

## 演进策略
- MVP 阶段优先单集群部署
- 随业务增长拆分高吞吐服务与高风险治理服务
- 观测、审计和成本服务优先保持独立部署边界
